---
'@luke-ui/react': minor
---

Export the fixed `breakpoints` object from `@luke-ui/react/styles` so consumers can author
`@container` queries against the same thresholds Luke UI responsive props use.

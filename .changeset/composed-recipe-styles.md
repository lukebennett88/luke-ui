---
'@luke-ui/react': patch
---

Shrink the published stylesheet by letting a recipe slot compose a shared class instead of duplicating a style object. The combobox's two action buttons now share one emitted block rather than one each.

---
'@luke-ui/react': patch
---

Shrink the published stylesheet: a recipe slot can now compose a shared class instead of duplicating
a style object, so the combobox's two action buttons share one emitted block.

---
'@luke-ui/react': minor
---

Emit Luke UI-owned scalable geometry tokens (`space`, typography, radius, icon and control sizes,
Container `ct*` max-widths) as `rem` against a 16px root so they follow the user's root font size.
Theme authoring still uses pixel-equivalent numeric design units.

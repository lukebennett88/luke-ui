---
'@luke-ui/react': minor
---

Rename space scale keys from numbered steps to `sp`-prefixed pixel sizes.

`gap="400"` is now `gap="sp16"`. `vars.space[400]` is now `vars.space.sp16`. Pixel values are
unchanged.

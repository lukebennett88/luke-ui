---
'@luke-ui/react': minor
---

Derive hover and pressed colours from resting semantic colours. Public tokens keep persistent
semantic colours only. `color.scrim` becomes `color.overlay.backdrop`. Hover and pressed leaves are
gone; first-party recipes mix toward `color.text.primary` internally.

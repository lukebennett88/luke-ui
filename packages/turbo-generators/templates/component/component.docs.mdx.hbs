`{{pascalCase name}}` from `@luke-ui/react/{{kebabCase name}}`.

## Import

```ts
import { {{pascalCase name}} } from '@luke-ui/react/{{kebabCase name}}';
```

## Usage

## Props

| Prop        | Type     | Default      | Notes |
| ----------- | -------- | ------------ | ----- |
| `children`  | `ReactNode` |             |       |
| `className` | `string` |              |       |
| `color`     | `string`  | `'neutralBold'` | Foreground color token. |
| `size`      | `'small' \| 'medium' \| 'large'` | `'medium'` | Size variant. |

`{{pascalCase name}}` from `@luke-ui/react/{{kebabCase group}}`.

## Import

```ts
import { {{pascalCase name}} } from '@luke-ui/react/{{kebabCase group}}';
```

## Usage

## Props

| Prop        | Type     | Default      | Notes |
| ----------- | -------- | ------------ | ----- |
| `children`  | `ReactNode` |             |       |
| `className` | `string` |              |       |
| `color`     | `string`  | `'neutralBold'` | Foreground color token. |
| `size`      | `'small' \| 'medium' \| 'large'` | `'medium'` | Size variant. |

export { Button, useButton } from './button';

/** @todo export this from a different entrypoint. */
export { docs as buttonDocs } from './button.docs';

// types

export type { ButtonProps, UseButtonProps } from './button';

import type { JSX } from 'react';

/** First primitive kit export. */
export function KitControl(): JSX.Element {
	return <div />;
}

/** Second primitive kit export. */
export function KitItem(): JSX.Element {
	return <div />;
}

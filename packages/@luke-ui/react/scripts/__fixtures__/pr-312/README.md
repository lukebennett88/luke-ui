# PR #312 checkbox captures

`checkbox-states-main.png` and `checkbox-states-current.png` are the real base/current pair from
[PR #312](https://github.com/lukebennett88/luke-ui/pull/312)'s visual regression run, recovered from
its GitHub Actions artifact. They are the historical case the old gate scored as `unchanged` despite
a visible icon change. Frozen historical data, not a generated screenshot, so it does not conflict
with `docs/VISUAL_TESTING.md`'s "no committed screenshots" policy for day-to-day visual testing.

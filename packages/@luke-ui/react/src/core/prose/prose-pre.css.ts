import { styleInLayer } from '../styles/layered-style.css.js';

/** Marks the keyboard-focusable scroll wrapper around a Prose code block. */
export const prosePreScrollClassName = styleInLayer('recipes', {}, 'prosePreScroll');

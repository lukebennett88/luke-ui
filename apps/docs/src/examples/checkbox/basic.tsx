import { Checkbox } from '@luke-ui/react/checkbox';

export default function Basic() {
	return (
		<Checkbox description="You can change this later in account settings.">
			Product updates
		</Checkbox>
	);
}

---
title: Typography
description: Use the active theme's type scale through semantic typography components.
---

The active theme owns the type scale. Each step combines font size, line height, letter spacing, and
Capsize trims. Use the typography components to keep those values together.

## Base components

Use `Text` for body copy and other non-heading content. Use `Heading` for document and section
headings. Heading manages semantic levels and selects a matching type step by default.

```tsx
import { Heading } from '@luke-ui/react/heading';
import { Text } from '@luke-ui/react/text';

<>
	<Heading>Account settings</Heading>
	<Text size="400">Choose how this account appears to other people.</Text>
</>;
```

Use `Numeral` for locale-aware values. It inherits the type scale. It formats currencies,
percentages, units, and compact values. Use `Emoji` when an emoji needs a reliable accessible label.

## Type scale

Text and Heading use the steps `100` through `900`. A `size` changes the whole type treatment,
rather than a font size alone.

The specimen keeps each step's font size, line height, letter spacing, and trim together.

<ExampleBlock src="text/typography" title="Text — Type scale" />

Text defaults to `300`. Heading selects a size from its level, and accepts `size` when the visual
hierarchy needs a deliberate override.

## Choose a size step

Pick a step for the role the content plays, not for a pixel measurement. The low steps, `100` and
`200`, suit compact labels and metadata that sit beside other content. The middle steps, `300`
through `500`, carry running text and short supporting copy. The high steps, `600` through `900`,
suit display text and prominent standalone values. A custom theme can change every step's exact
size, so the role is what stays true across themes.

## Capsize trims

Every step also carries a Capsize trim. A line box normally reserves space above the capital letters
and below the baseline, left over from the font's internal metrics. The trim removes that reserve,
so a text box's own edges land on the cap height and the baseline instead.

Trimmed text keeps a predictable baseline, regardless of line height. Two very different size steps
land on the same line when trimmed. Left untrimmed, the reserved space pushes each step's letters a
different distance above it.

<CapsizeTrimExplorer />

## Weight and colour

Themes define the `body`, `label`, `heading`, and `emphasis` weight roles. Use the role that
describes the content, instead of a literal numeric weight. Text also accepts semantic `color`
values such as `primary`, `secondary`, `accent`, `info`, `success`, `warning`, and `danger`.

```tsx
<Text color="secondary" fontWeight="label" size="200">
	Last updated 12 minutes ago
</Text>
```

Applications load any font files required by a custom theme. The theme then assigns that typeface to
the shared scale and weight roles.

## Best practices

| Guidance | Practice                                                                                            |
| -------- | --------------------------------------------------------------------------------------------------- |
| Do       | Choose a size step for the content's role, such as `100` for metadata or `700` for a display value. |
| Don't    | Pick a size step by comparing pixel measurements between themes.                                    |
| Do       | Use `Heading` levels to build document structure, and reach for `size` only as a visual override.   |
| Don't    | Skip a heading level to get a smaller size. Set `size` on the correct level instead.                |

## Continue learning

<Cards>
	<Card href="/components/typography/text" title="Text">
		Style body content with semantic colours and coordinated type steps.
	</Card>
	<Card href="/components/typography/heading" title="Heading">
		Create semantic headings with automatic level management.
	</Card>
	<Card href="/components/typography/numeral" title="Numeral">
		Format locale-aware numbers consistently.
	</Card>
	<Card href="/components/typography/emoji" title="Emoji">
		Give meaningful emoji an accessible label.
	</Card>
</Cards>

---
title: Text Field
description: Captures a single line of text with a label, validation, and optional adornments.
---

Use `TextField` to collect a single line of text. It combines an input with a label, description,
and validation message.

<ExampleBlock
	src="text-field/basic"
	title="Text Field — Basic"
	description="A labelled email field with supporting text."
/>

## Labels and placeholders

Give the field a visible `label` where possible. Use `placeholder` for an example value or input
format, not as the label. A placeholder disappears while the person types and is not a reliable
accessible name.

When a nearby heading already names the field, omit `label` and use `aria-label` or
`aria-labelledby` instead.

```tsx
<TextField aria-label="Search" name="search" placeholder="Search" />
```

## Required fields

Set `isRequired` for required fields. `necessityIndicator="icon"` adds a visual marker.
`necessityIndicator="label"` appends “(required)” to the label.

<ExampleBlock
	src="text-field/required"
	title="Text Field — Required"
	description="Required fields with icon and label indicators."
/>

## Validation

Pass `errorMessage` with `isInvalid` to report a problem. Always pass both: an invalid field with no
message leaves someone who cannot see the border colour with no way to know what is wrong.

<ExampleBlock
	src="text-field/validation"
	title="Text Field — Validation"
	description="An invalid email field with a message that names the fix."
/>

Read [Validation](/components/forms/validation) for server validation and for how to word the
message.

## Adornments

Use `adornmentStart` and `adornmentEnd` for content inside the input, such as a search icon,
currency code, or URL prefix. Both accept any `ReactNode`. Give interactive adornments an accessible
name and keyboard behaviour.

<ExampleBlock
	src="text-field/adornments"
	title="Text Field — Adornments"
	description="Icons and text before or after the input value."
/>

## Size

Use `size` to set the input height and typography. Use `small` in compact layouts. `medium` is the
default. The HTML numeric `size` attribute is unavailable because `size` is the Luke UI variant.

<ExampleBlock
	src="text-field/sizes"
	title="Text Field — Size"
	description="Small and medium text fields."
/>

## Primitive

Use the [InputGroup primitives](/components/primitives/input-group) when you need an input without
the label, description, and error slots, or a composition `adornmentStart` and `adornmentEnd` cannot
express.

---
title: Link
description: A styled link for inline and standalone navigation.
source: packages/@luke-ui/react/src/exports/link.ts
reactAria: https://react-spectrum.adobe.com/react-aria/Link.html
---

`Link` navigates to another URL or route. Use [`Button`](/components/actions/button) for an action
such as saving, submitting, or opening a dialog.

<ExampleBlock src="link/basic" title="Link — Basic" />

## Tone

`accent` is the default tone. Use `neutral` for supporting links that should not compete with the
main content.

<ExampleBlock src="link/tones" title="Link — Tone" />

## Inline and standalone

Use the default treatment inside a sentence. Its underline stays visible so the destination is
recognisable in running text.

Set `isStandalone` for a link outside prose, such as a card link or navigation item. It shows its
underline on hover or press and has a 24 × 24 CSS-pixel structural target.

<ExampleBlock src="link/standalone" title="Link — Inline and standalone" />

## Disabled

Set `isDisabled` only when a destination is temporarily unavailable. Disabled links cannot receive
focus or respond to activation. Explain why nearby when someone needs that information.

<ExampleBlock src="link/disabled" title="Link — Disabled" />

## Custom styling

Pass `xstyle` with one or more `stylex.create(...)` style objects for styles `Link`'s own props do
not cover, or to override a same-property variant. Styles apply in this order: variant props, then
`xstyle`, then `className`, then inline `style`. A same-property `xstyle` value wins over a variant
such as `tone`. `className` still beats `xstyle`.

## Accessibility

Write link text that names the destination and makes sense outside its surrounding sentence. Avoid
generic labels such as “Click here”.

## API

<component-props-table path="packages/@luke-ui/react/src/core/link/link.tsx" name="LinkProps" />

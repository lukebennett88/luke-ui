---
title: Link
description: A styled link for inline and standalone navigation.
---

`Link` navigates to another URL or route. Use [`Button`](/components/actions/button) for an action
such as saving, submitting, or opening a dialog.

<ExampleBlock src="link/basic" title="Link — Basic" description="A link to another destination." />

## Tone

`accent` is the default tone. Use `neutral` for supporting links that should not compete with the
main content.

<ExampleBlock
	src="link/tones"
	title="Link — Tones"
	description="Accent is the default. Neutral recedes within surrounding content."
/>

## Inline and standalone links

Use the default treatment inside a sentence. Its underline stays visible so the destination is
recognisable in running text.

Set `isStandalone` for a link outside prose, such as a card link or navigation item. It shows its
underline on hover or press and has a 24 × 24 CSS-pixel structural target.

<ExampleBlock
	src="link/standalone"
	title="Link — Inline and standalone"
	description="Keep links in prose inline; use standalone treatment for a separate action."
/>

## Disabled links

Set `isDisabled` only when a destination is temporarily unavailable. Disabled links cannot receive
focus or be activated. Explain why nearby when that information is needed.

<ExampleBlock
	src="link/disabled"
	title="Link — Disabled"
	description="Disabled links remain visible but cannot be activated."
/>

## Accessibility

Write link text that names the destination and makes sense outside its surrounding sentence. Avoid
generic labels such as “Click here”.

---
title: Prose
description: Vertical rhythm and list structure for long-form content.
source: packages/@luke-ui/react/src/exports/prose.ts
props:
  - name: ProseProps
    path: packages/@luke-ui/react/src/core/prose/prose.tsx
---

`Prose` applies a fixed document rhythm to rendered Markdown, MDX, or content management system
output. It restores document list structure. It sets no typography, colour, weight, type scale, or
width, and adds no space outside itself. Constrain the measure on a surrounding layout.

<ExampleBlock src="prose/basic" title="Prose — Basic" />

## Lists

The Luke UI reset strips markers and indent from lists because interface lists are often menus.
`Prose` restores them for documents, including nested lists.

<ExampleBlock src="prose/lists" title="Prose — Lists" />

## Overriding the rhythm

Every rule has zero specificity, so a plain class on an element inside `Prose` can adjust one block
without `!important` or a longer selector.

```tsx
<Prose>
	<figure className={styles.fullBleedFigure}>
		<img alt="" src="/diagram.png" />
	</figure>
</Prose>
```

## Related components

Use [Text](/components/typography/text) and [Heading](/components/typography/heading) directly when
you author the markup and want to choose each gap yourself.

---
title: Code
description: Marks text to signify a short fragment of computer code.
---

This component is based on the `code` element.

<ExampleBlock src="code/basic" title="Code — Basic" description="A short inline code fragment." />

## In context

`Code` renders as an inline element, so it sits naturally inside a sentence.

<ExampleBlock
	src="code/in-context"
	title="Code — In context"
	description="Code markers within a paragraph."
/>

## Accessibility

Uses the native `<code>` element, which conveys code semantics to assistive technologies.

---
title: Kbd
description: Represents keyboard input or a hotkey.
---

<ExampleBlock
	src="kbd/basic"
	title="Kbd — Basic"
	description="Keyboard shortcuts within a paragraph."
/>

`Kbd` is based on the native `kbd` element and uses the fixed code font family.

## Accessibility

Uses the native `<kbd>` element, which conveys keyboard-input semantics to assistive technologies.
